<p class="fs-2">Asignar turno a la sucursal <%= @subsidiary.name_subsidiary %> </p>

<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" />
   <hr class="bg-danger border-2 border-top border-danger">
</p>

<p><em> Seleccione el dia de la semana y el horario</em></p>

<%= form_for @schedule, url: subsidiary_schedules_path, html: { method: :post } do |form| %>
   <%= render 'form', form: form %>
<% end %>

<%= link_to 'Volver', subsidiaries_path , class: 'btn,btn-secondary' %>
