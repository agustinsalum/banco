<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    
    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto">

        <%= link_to root_path do %>
          <%= image_tag "house.svg", size: "30x30", class: "some class here" %>
        <% end %>

        <% if current_user %>
          <li class="nav-item">
            <button type="button" class="btn btn-dark"> <%= link_to "Mis turnos", turns_path %> </button>
          </li>

          <li class="nav-item">
            <button type="button" class="btn btn-dark"> <%= link_to "Gestionar Usuarios", users_path %> </button>
          </li>

          <li class="nav-item">
            <button type="button" class="btn btn-dark"> <%= link_to "Gestionar Sucursales", subsidiaries_path %> </button>
          </li>

          <% if can? :manage, @locality %> 
            <li class="nav-item">
              <button type="button" class="btn btn-dark"> <%= link_to "Gestionar Localidades", localities_path %>  </button>
            </li>
          <% end %>

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown"  role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.name %></a>
            <div class="dropdown-menu">
              <a class="dropdown-item" href="#"><%= link_to "Ver perfil", perfil_users_path %></a>
              <div class="dropdown-divider"></div>
              <a class="dropdown-item" ><%= button_to "Cerrar sesion", destroy_user_session_path, method: :delete, data: { turbo:false } %></a>
           </div>
         </li>
        <% end %>
       </ul>
     </div>
   </div>
</nav>